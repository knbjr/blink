#define LED 3
void setup (){
  pinMode (LED, OUTPUT);
  
}

void loop () {
  digitalWrite(LED, HIGH);   // turn the LED on (HIGH is the voltage level)
  delay(100);               // wait for second
  digitalWrite(LED, LOW);    //turn the LED off by matching the voltage LOW
  delay(100);               //wait for second
  
}
